<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CinemaMobileClient.Views.MenuPage"
             xmlns:local="clr-namespace:CinemaMobileClient.Views">
    <TabBar>
        <Tab Title="Home" Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate local:HomePage}" />
        </Tab>
        <Tab Title="Cines" Icon="cines.png">
            <ShellContent ContentTemplate="{DataTemplate local:CinesPage}" />
        </Tab>
        <Tab Title="Confitería" Icon="palomitas.png">
            <ShellContent ContentTemplate="{DataTemplate local:ConfiteriaPage}" />
        </Tab>
        <Tab Title="Perfil" Icon="perfil.png">
            <ShellContent ContentTemplate="{DataTemplate local:PerfilPage}" />
        </Tab>
    </TabBar>
</Shell>